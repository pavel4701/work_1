Составить программу в которой переменной t логического типа, присваивается Тгuе, если целый параметр К является четным, и False в противном случае (не использовать if, а только логическую переменную).

from math import *

k = int(input("Vvedite k: "))

c = k%2

print ("4etnoe li 4islo: ", c == 0)
